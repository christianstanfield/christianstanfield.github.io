$(function () {

  // Init Skrollr
  var s = skrollr.init({
    forceHeight: false
  });


  // Get window size
  // var winH = $(window).height();
  //
  // // Keep minimum height 550
  // if(winH <= 550) {
  //   winH = 550;
  // }

  // Resize our slides
  // $('.slide').height(550);

  // Refresh Skrollr after resizing our sections


});
